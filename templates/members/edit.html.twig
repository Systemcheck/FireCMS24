{% extends params.tpl~'base.html.twig' %}

{% block title %}{{sitename}}{% endblock %}
{% block body %}
    <div class="row">
        <div class="col-sm-12">
        <h2>Datensatz "{{firstname}} {{lastname}}" bearbeiten</h2></div>
    </div>

    <div class="card mb-3">
        <div class="card-header">
        <i class="fas fa-table"></i>
        Bearbeiten</div>
        <div class="card-body">
            <ul class="nav nav-tabs" id="myTab" role="tablist">
                <li class="nav-item">
                    <a class="nav-link active" id="home-tab" data-toggle="tab" href="#home" role="tab" aria-controls="home" aria-selected="true">Persönliche Daten</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" id="profile-tab" data-toggle="tab" href="#profile" role="tab" aria-controls="profile" aria-selected="false">Profile</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" id="contact-tab" data-toggle="tab" href="#contact" role="tab" aria-controls="contact" aria-selected="false">Contact</a>
                </li>
            </ul>
 {{ form_start(editUserForm) }}
<div class="tab-content" id="myTabContent">
  <div class="tab-pane fade show active" id="home" role="tabpanel" aria-labelledby="home-tab">
    <div class="row">
        <div class="col-md-3">
        {{ form_label(editUserForm.firstname, 'Vorname') }}
        {{ form_widget(editUserForm.firstname, {'attr': {'class': 'form-control' }},{value : 'Default title'}) }}
        </div>
        <div class="col-md-3" id="some-custom-id">
        {{ form_label(editUserForm.lastname, 'Nachname') }}
        {{ form_widget(editUserForm.lastname, {'attr': {'class': 'form-control' }}) }}
        </div>
        <div class="col-md-3" style="border:1px solid primary">
        image container
        </div>
    </div>
    <div class="row">
        <div class="col-md-6">
        {{ form_label(editUserForm.street, 'Anschrift') }}
        {{ form_widget(editUserForm.street, {'attr': {'class': 'form-control' }}) }}
        </div>
    </div>
    <div class="row">
        <div class="col-md-2">
         {{ form_label(editUserForm.postcode, 'PLZ') }}
         {{ form_widget(editUserForm.postcode, {'attr': {'class': 'form-control' }}) }}
        </div>
        <div class="col-md-4">
        {{ form_label(editUserForm.city, 'Wohnort') }}
        {{ form_widget(editUserForm.city, {'attr': {'class': 'form-control' }}) }}
        </div>
    </div>


        
          
  
  </div>
  <div class="tab-pane fade" id="profile" role="tabpanel" aria-labelledby="profile-tab">
    <div class="row">
        <div class="col-md-3">
        {{ form_label(editUserForm.firstname, 'Vorname') }}
        {{ form_widget(editUserForm.firstname, {'attr': {'class': 'form-control' }},{value : 'Default title'}) }}
        </div>
        <div class="col-md-3" id="some-custom-id">
        {{ form_label(editUserForm.lastname, 'Nachname') }}
        {{ form_widget(editUserForm.lastname, {'attr': {'class': 'form-control' }}) }}
        </div>
        <div class="col-md-3" style="border:1px solid primary">
        image container
        </div>
    </div>
  </div>
  <div class="tab-pane fade" id="contact" role="tabpanel" aria-labelledby="contact-tab">...</div>
</div>
<div class="row"><div class="col"><br /><button class="btn-small btn-primary">Speichern</button></div></div>
{{ form_end(editUserForm) }}

         
           
        </div>
        <div class="card-footer small text-muted">Updated</div>
    </div>

    
{% endblock %}